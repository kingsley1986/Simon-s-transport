<%= form_with(model: citysuburb, local: true) do |form| %>
  <% if citysuburb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(citysuburb.errors.count, "error") %> prohibited this citysuburb from being saved:</h2>

      <ul>
        <% citysuburb.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
